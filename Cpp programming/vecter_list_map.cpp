#include <iostream>
#include <vector>
#include <list>
#include <map>
#include <cstring>
#include <functional>
using namespace std;
int main()
{
    //vector<int> a; //it is like array
    //list <int > a;
    //list<int>::iterator it=a.begin();

    // a.push_back(12);
    // a.push_back(112);
    // a.push_back(11);
    // int n;
    // for (int i = 0; i < 6; i++)
    // {
    //     cin >>n;
    //    a.push_back(n);//it is use to add value in vecter
    // }

    // for (int  i = 0; i < a.size()/*it is size*/; i++)
    // {
    //     cout<<a[i]<<"  ";
    // }

    // vector<int> a = {12, 11, 12112, 11, 21, 122};
    // vector<int>:: iterator avi;
    // avi=a.begin();
    // a.pop_back();
    //a.insert(avi+2,12,12);
    // for (int i = 0; i < a.size() /*it is size*/; i++)
    // {
    //     cout << *avi << "  ";
    //     avi++;
    // }
    // map<string , int> map112;
    // map112["avinash"] = 98;
    // map112["avih"] = 988;
    // map112["nash"] = 8;
    // map112<string , int>::iterator iter;
    // cout << (*iter).first << "  " << (*iter).second;

    return 0;
}
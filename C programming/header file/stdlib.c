#include <stdio.h>
#include <stdlib.h>
#include <windows.h>

int main()
{
    //system("cls")---->it is use for cleering output
    //print new output
    // printf("before system(cls)\n\n ");
    // Sleep(1200);
    // system("cls");
    // printf("after system(cls)\n ");

    // system("pause");---->it is use pause the screen until press any key

    // printf("before system(cls)\n\n ");
    // Sleep(1200);
    // system("cls");
    // printf("after system(cls)\n ");
    // system("pause");

    // atexit()--->it is give which does last executing function
    // void a(){
    //     printf("a ");
    // }
    // void b(){
    //     printf("b ");
    // }
    // int main(){
    //     atexit(a);//3rd
    //     b();      //1st
    //     printf("c");
    // }
    // o\p---b
    //       c
    //       a

    //atof()--->convert string into float
    //atol()--->convert string into long
    // char s[12] = "12345";
    // float a;
    // a = atof(s);
    // printf("%f\n", a);
    //atoi()--->convert string into int
    // char s[12] = "12345";
    // int a;
    // a = atof(s);
    // printf("%d\n", a);

    // random no.
    // srand(time(0));
    // rand()%10+1;-->10 to 1 random

    // ads()--->it is use for make positive no.
    //          p-->n
    //          n-->p
    // int a=-12.333;
    // printf("%d\n", abs(a));

    // int a=rand()%10;//-->it is give one random no.
    // printf("%d",a);

    // int x,y ,i=0;
    // while(i<40){
    // label1://--->decleration of label it is use colon(:)
    // x=rand()%20;//-->it ganrate 0 to 19 random no.
    // printf("%d\n",x);
    // if(x==10){
    // goto label1;//-->it is use for goto label
    // }
    // i++;
    // }

    // div_t , div --->it is return remainder and quotient
    //     ex div_t r=div(56,4);
    // div_t r=div(34,2);
    // printf("%d\n",r.quot );
    // printf("%d\n",r.rem );

    //system("color a1");---->it is use for changing color of text and background
    //system("color 3");----->it is use for changing text color
    //system("color a");
    //printf("before system(cls)\n\n ");
    
    return 0;
}
